<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FindTheTreasure.Pages.Game.CreateGameView"
             x:DataType="viewmodel:CreateGameViewModel"
             xmlns:viewmodel="clr-namespace:FindTheTreasure.Pages.Game">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Entry x:Name="Game name"
               Text="{Binding GameModel.Name}"
               Placeholder="Game name"
               VerticalOptions="Center"
               HorizontalOptions="Center"
               Grid.Row="1"/>
        <Button Text=""
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Command="{Binding CreateGame}"
                Grid.Row="6"/>
    </Grid>
</ContentPage>